// Посмотреть версию прошивки
i("PrintStatus.js");
PrintFWInfo();

// Обновление прошивки
i("FWUpdate.js");
FWU_CS();

// Скачать дамп регистров
i("FWUpdate.js");
FWU_DumpCS();

// Загрузить дамп регистров
i("FWUpdate.js");
FWU_RestoreCS();

// Включить нагрев до 200 град. ц.
dev.w(72, 2000);
dev.c(108);

// Функция сбора данных о температуре
include("TestCS.js");				// Подключение скрипта 
CS_Temp(10000);					// 

// Калибровка ЦАП температуры (!!!Нагрев пресса не нужен)
include("CalCS.js");				// Подключение скрипта 
ccs_t_min = 80;					// Выставление необходимого минимального значения температуры 
ccs_t_max = 190;				// Выставление необходимого максимального значения температуры
ccs_t_stp = 5;					// Выставление значения шага температуры
CCS_TempToDACCalibrate(); 			// Калибровка ЦАП температуры 
CCS_TempToDACVerify(); 				// Верификация ЦАП температуры

// Калибровка измерения температуры
Нагреть до максимальный температуры (190-200 градусов)
include("CalCS.js");
CCS_TempReadCalibrate([TRM_AddrArray]) // Для штыревых ЗУ: [1] - основание, [2] - хвост; для таблеточных ЗУ: [1, 2] - обе нагревательные пластины 

// Калибровка усилия зажатия
include("CalCS.js");			 	// Подключение скрипта 
dev.co(12); 					// Подключение к ЗУ	
force_min = 5; 					// Выставление необходимого минимального значения усилия зажатия 
force_max = 95;					// Выставление необходимого максимального значения усилия зажатия
force_step = 5;					// Выставление значения шага усилия зажатия
PortNumberTerminal = 7;				// COM порт терминала
mode_force = 's';				// 'a' автоматический ввод задания, 's' - ручной ввод в консоль
mode_terminal = 's';				// 'a' автоматическое снятие данных с терминала, 's' - ручной ввод кг в консоль

CCS_ClampADCOffsetCalibrate(); 			// Выставление 0 при считывании усилия зажатия  
CCS_ClampCalibrate(); 				// Калибровка усилия зажатия  
CCS_ClampVerify(); 				// Верификация усилия зажатия 
 
